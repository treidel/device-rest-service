jar {
	baseName = 'message-rabbitmq'
	version = "$version"
}

dependencies {
	compile("com.rabbitmq:amqp-client:$rabbitmqVersion")	
	testCompile("org.springframework:spring-test:$springVersion")        	
	testCompile("junit:junit:$junitVersion")
}

