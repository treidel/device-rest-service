jar {
	baseName = 'storm-kinesis'
	version = "$version"
}

dependencies {
	compile("com.amazonaws:aws-java-sdk-kinesis:$amazonAwsVersion")	
	compile("com.amazonaws:kinesis-storm-spout:$amazonKinesisStormSpoutVersion")
	testCompile("junit:junit:$junitVersion")
}

