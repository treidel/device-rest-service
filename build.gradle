subprojects {
	apply plugin: 'java'
	group = 'com.test.device'
	version = '1.0'
}


project(':device') {
	dependencies {
		compile project(':data')
	}
}
project(':app') {
	dependencies {
		compile project(':data')
	}
}
