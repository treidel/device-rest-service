jar {
	baseName = 'device'
	version = "$version"
}

dependencies {
	compile("org.springframework.security:spring-security-core:$springSecurityVersion")
	compile("org.apache.james:apache-mime4j-core:$apacheJamesVersion")
	compile("org.apache.james:apache-mime4j-dom:$apacheJamesVersion")
}

