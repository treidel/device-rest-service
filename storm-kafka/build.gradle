jar {
	baseName = 'storm-kafka'
	version =  '1.0.0'
}

dependencies {
	compile("org.apache.kafka:kafka_2.10:$kafkaVersion")
	compile("org.apache.storm:storm-kafka:$apacheStormVersion")
	testCompile("junit:junit:$junitVersion")
}

